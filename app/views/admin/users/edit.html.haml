#page-wrapper
  #page-inner
    .row
      .col-md-12
        %h2= page_header
    %hr
    .row
      .col-md-12
        .panel.panel-default
          .panel-heading
            Edit the #{page_header_singularized}
          .panel-body
            .row
              .col-md-6
                = simple_form_for [:admin, @user] do |f|
                  = f.input :username
                  = f.input :email
                  = f.input :group_id, collection: UserGroup.all.pluck(:name, :id), include_blank: false
                  = f.input :admin, as: :boolean
                  = f.button :submit, 'Edit', class: 'btn btn-success'
